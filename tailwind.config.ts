import type { Config } from 'tailwindcss';

export default {
  theme: {
    extend: {
      colors: {
        background: '#F1D9C6',
        darkBrown: '#4f341b',
        foreground: '#EBCDB5',
      },
      fontFamily: {
        // sans: ['var(--font-geist-sans)'],
        // mono: ['var(--font-geist-mono)'],
        
      },
    },
  },
} satisfies Config;